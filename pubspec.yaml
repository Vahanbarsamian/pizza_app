name: pizza_app
description: "Application Pizza - Android/Web"
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.2.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  supabase_flutter: ^2.5.0
  flutter_stripe: ^10.0.0
  flutter_map: ^6.1.0
  latlong2: ^0.9.0
  http: ^1.1.0
  url_launcher: ^6.2.1
  drift: ^2.17.0
  sqlite3_flutter_libs: ^0.5.21
  path_provider: ^2.0.0
  path: ^1.9.0
  flutter_dotenv: ^5.1.0
  provider: ^6.1.2
  font_awesome_flutter: ^10.7.0
  # ✅ NOUVEAU: Pour optimiser le chargement des images
  cached_network_image: ^3.3.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
  drift_dev: ^2.17.0
  build_runner: ^2.4.7

flutter:
  uses-material-design: true
  assets:
    - .env
    - assets/images/
