import 'package:flutter/material.dart';

class CompanyInfoEditScreen extends StatelessWidget {
  const CompanyInfoEditScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Modifier les informations')),
      body: const Center(child: Text('Écran à implémenter')),
    );
  }
}
